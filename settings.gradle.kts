pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "UnResolvedReferenceBug"
include(":androidApp")
include(":shared")